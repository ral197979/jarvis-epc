# Denver Engineering Next — environment

# Use bundled mock data (default). Set to "false" to call the live Denver API.
VITE_USE_MOCKS=true

# Base URL for the existing Denver Engineering API (used when VITE_USE_MOCKS=false).
VITE_API_BASE=/api/v1
