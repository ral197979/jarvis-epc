# PR Draft — Close P0/P1 audit findings with runtime proof and regressions

**Branch:** `fix/audit-p0-p1-closure-2026-07-02` → `main`
**Commit:** `97befa6`

This is a draft only — no PR was opened and nothing was pushed. Paste this into `gh pr create` (or the GitHub UI) once you're ready.

---

## Title
```
Close P0/P1 audit findings with runtime proof and regressions
```

## Description

### Summary
This PR closes the prioritized P0/P1 audit findings with code fixes, regression coverage, and live runtime verification. It also documents three intentionally deferred issues that require separate product/security decisions rather than silent remediation.

### Closed
- P0-01/02 plus `personalAgentRouter` `/api/v1` default-config 404 root cause (discovered during closure — see evidence doc for details; this is likely the actual root cause of "My Work fails to load" in real deployments, not just the local dev-environment causes).
- P0-03 duplicate `change_orders` schema reconciliation (additive migration, no data loss).
- P0-04/05 EVM `evm_actuals`/`evm_baselines` column mismatch fixes.
- P0-06 `DATABASE_URL_APP` production fail-closed behavior.
- P0-07 SAML metadata SSRF guard.
- P0-08 notification stubs report real failure instead of fake success.
- P0-09 fatal exit paths flush logs before `process.exit`.
- P1 role gates (change-order approve/reject, enterprise demo/health, federated-patterns publish) and server-side k-anonymity computation.
- P0-10 `useBizStore` project hydration path — closed for `projects`, pattern established for future collections.

### Verification
- Typecheck clean for both configs (`tsc --noEmit`, `tsc --project tsconfig.modules.json --noEmit`).
- Full test suite passing: **5,298 / 5,298** (157/157 files).
- Lint unchanged: 32 pre-existing warnings, 0 new (verified identical file-for-file against `main`).
- Monolith gate passing (`JarvisCore.jsx`: 1,134 lines).
- `npm audit --audit-level=high` clean (1 pre-existing low-severity dev-only advisory, unchanged).
- Production build passing.
- Live end-to-end proof: real Vite dev server → real Express API → real Postgres, verified via direct `curl` against all three fix layers.
- Evidence: [`audit/evidence/CLOSURE_EVIDENCE_2026-07-02.md`](../evidence/CLOSURE_EVIDENCE_2026-07-02.md) — includes a full reviewer mapping table (finding → fix → test → proof → status) and before/after `git stash` bisection proof for every testable fix.

### Deferred by design (not silently omitted — see evidence doc's "Explicitly left open" section and the three follow-up issue drafts)
- Complete `BizState` hydration expansion (25 of 26 collections still unhydrated).
- Complete RLS `query()` enforcement sweep (76 of 91 call sites still bypass `tenantQuery()` even with correct config).
- Resolve `budgets.ts` vs `changeOrders.ts` router path collision by product/API decision — discovered during this work, requires a product call, not a silent route change.

### Reviewer focus
- Migration safety for `change_orders` (additive-only, verify against the `083_reconcile_change_orders.sql` header comment).
- `/api/v1` router behavior when `PERSONAL_AGENT` is unset — this is the highest-impact fix in the PR.
- `DATABASE_URL_APP` fail-closed production behavior — confirm your deployment target (Render/Fly/etc.) has this variable set *before* deploying this change, or production boot will intentionally fail.
- SSRF guard correctness (`assertSafeUrl` reuse, not new guard logic).
- Role gate coverage and the chosen role sets (`owner`/`admin`/`project_manager` for change-order approval — not product-confirmed, see evidence doc residual risk note).
- Evidence file accuracy (cross-check a sample of the `git stash` bisection claims yourself if you want independent confirmation).
- Deferred issue boundaries — confirm you agree these three are genuinely out of scope for this PR and worth separate tickets.

### Note on branch contents
Two files (`api/services/schedule/mspParser.ts`, `api/services/schedule/xerParser.ts`) and one new test file (`api/__tests__/scheduleParsers.test.ts`) are present in the working tree but **were deliberately left out of this commit** — they're unrelated, pre-existing, ready-to-commit work (an XER/MSPDI date-parsing fix) that predates this remediation branch. See `git status` on this branch; they'll show as uncommitted changes. Recommend committing them separately.

---

## Reviewer checklist
- [ ] Evidence file matches actual code and test changes.
- [ ] `personalAgentRouter` no longer breaks `/api/v1` when `PERSONAL_AGENT` is unset.
- [ ] `change_orders` migration is additive and non-destructive.
- [ ] EVM actuals/baselines queries match schema.
- [ ] `DATABASE_URL_APP` fails closed in production when RLS cannot be enforced.
- [ ] SAML metadata import rejects unsafe/internal URLs.
- [ ] Notification stubs no longer return fake success.
- [ ] Fatal exit paths flush logs before `process.exit`.
- [ ] Sensitive P1 routes have role gates.
- [ ] k-anonymity is computed server-side.
- [ ] Project hydration regression is covered.
- [ ] Deferred issues are documented as follow-up work, not hidden omissions.
- [ ] Confirm `DATABASE_URL_APP` is set in every production deployment target *before* merging (this PR will hard-block boot otherwise — see P0-06).
- [ ] Confirm the two unrelated schedule-parser files are intentionally excluded from this PR.

---

## Follow-up issues to file after this PR merges

See `audit/evidence/CLOSURE_EVIDENCE_2026-07-02.md`'s "Follow-up issues" section for full drafts of:
1. Complete BizState Hydration Coverage Beyond Projects
2. Complete RLS Enforcement Sweep for Remaining `query()` Call Sites
3. Resolve `budgets.ts` vs `changeOrders.ts` Router Path Collision

Not filed automatically — this repo's GitHub issue tracker wasn't confirmed reachable/authorized in this session. Say the word and I'll file them via `gh issue create` once you confirm the repo/remote.
